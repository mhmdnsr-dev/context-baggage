# v0.1

> Status: v0.1 milestone COMPLETE. All implemented v0.1 scope passed local verification, manual sync/conflict validation, workspace identity validation, privacy/secret audit, public module validation, and remote GitHub Actions CI. `v0.1.0` is published as the first public release with release validation PASS.

## M0 — Foundation

Status: Complete

Scope:

- Go module;
- `cmd/ctx-bag/main.go`;
- CLI root;
- app-data directory resolver;
- initialization;
- device identity;
- store foundation;
- baseline tests.

Completed:

- Phase 0 documentation bootstrap completed before M0.
- Required planning Markdown artifacts verified.
- Go module initialized.
- `cmd/ctx-bag/main.go` created.
- CLI root implemented.
- Application-data directory resolver implemented.
- `ctx-bag init` implemented.
- Device identity implemented.
- Store foundation implemented.
- Baseline tests implemented.

Verification:

- Required Markdown file existence check passed.
- `go test ./...` passed.
- `go vet ./...` passed.

Known Issues:

- None recorded.

Next Action:

- Complete M1.

## M1 — Workspace Identity

Status: Complete

Scope:

- Git root resolution;
- normalized repository identity;
- stable workspace ID;
- local path registration;
- workspace metadata;
- workspace status;
- tests.

Completed:

- Git root resolution implemented.
- Git remote identity normalization implemented for SSH and HTTPS forms.
- Credential stripping implemented for remote URLs.
- Stable workspace IDs implemented.
- Local path registration implemented.
- `ctx-bag workspace init` and `ctx-bag workspace status` implemented.
- `--sync` and `--no-sync` workspace policy options implemented.

Verification:

- Workspace unit tests passed.
- End-to-end workflow verifies equivalent SSH/HTTPS remotes resolve to the same workspace.

Known Issues:

- None recorded.

Next Action:

- Complete M2.

## M2 — Task / Checkpoint / Handoff

Status: Complete

Scope:

- task creation;
- active task state;
- resume;
- checkpoints;
- handoff;
- persistence;
- tests;
- begin dogfooding if practical.

Completed:

- Task creation implemented.
- Active task state implemented.
- Task resume implemented.
- JSONL checkpoints implemented.
- Markdown handoff template implemented.
- Persistence tests implemented.

Verification:

- Task/checkpoint/handoff tests passed.
- End-to-end workflow verifies task, checkpoint, and handoff survive sync pull.

Known Issues:

- None recorded.

Next Action:

- Complete M3.

## M3 — Agent Discovery

Status: Complete

Scope:

- agent adapter interface;
- Claude Code detection;
- Codex detection;
- sanitized configuration discovery;
- secret redaction;
- tests.

Completed:

- Agent adapter interface implemented.
- Claude Code detection implemented.
- Codex detection implemented.
- Sanitized high-level inventory persistence implemented.
- Secret redaction implemented.

Verification:

- Redaction tests passed.
- Full test suite passed.

Known Issues:

- None recorded.

Next Action:

- Complete M4.

## M4 — Folder Sync

Status: Complete

Scope:

- sync-folder configuration;
- push;
- pull;
- status;
- workspace exclusion;
- conflict detection;
- tests.

Completed:

- Sync-folder configuration implemented.
- Push implemented.
- Pull implemented.
- Status implemented.
- Workspace exclusion implemented.
- Conservative conflict detection implemented.

Verification:

- Sync unit tests passed.
- End-to-end two-machine simulation passed.

Known Issues:

- None recorded.

Next Action:

- Complete M5.

## M5 — Release Readiness

Status: Complete

Scope:

- test suite;
- documentation consistency;
- cross-platform builds where practical;
- examples;
- installation;
- release notes;
- `v0.1.0`.

Completed:

- Test suite completed for the implemented v0.1 surface.
- Documentation consistency updated after implementation.
- Local Linux build verified.
- Cross-compilation verified for Linux, macOS, and Windows amd64.
- `v0.1.0` implementation ready for release packaging.
- Pre-release cleanup completed.
- Development tooling configured.
- Manual test binary built at `./bin/ctx-bag`.

Verification:

- `go test ./...` passed.
- `go vet ./...` passed.
- `go build ./cmd/ctx-bag` passed.
- Cross-compilation passed for Linux amd64, macOS amd64, and Windows amd64.
- Pre-release verification passed with `go fmt ./...`, `go vet ./...`, `go test ./...`, `go mod tidy`, and `go build -o ./bin/ctx-bag ./cmd/ctx-bag`.
- Public fresh clone, `go install ...@latest`, and GitHub Actions CI (Go 1.22 + Go 1.27 + golangci-lint) all validated.

Known Issues:

- Cross-compiled binaries were not runtime-tested on macOS or Windows.
- Agent discovery is intentionally conservative and high-level.
- Git remote workspace display names previously used the first machine's local folder basename; fixed and validated before first commit.
- Non-Git cross-machine matching is intentionally not automatic in `v0.1`.

Next Action:

Post-v0.1 planning is handled separately.
